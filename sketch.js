function setup() {
  createCanvas(400, 400);
  background(255,204,240);
}

function draw() {  
                 
                 strokeWeight(2);
                 stroke (255, 20,2)
                 fill (255,204, 240)
                 circle(mouseX, mouseY, mouseX)
                 
}